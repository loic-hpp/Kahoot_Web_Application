/**
 * Interface to represent a match history
 */
export interface MatchHistory {
    matchAccessCode: string;
    bestScore: number;
    startTime: string;
    nStartPlayers: number;
    gameName: string;
}
