/**
 * Interface to represent to validate
 * if a player can join a match or not
 * with an access code and a name that
 * the player enters.
 */
export interface Validation {
    accessCode: string;
    name: string;
}
