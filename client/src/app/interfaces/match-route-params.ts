/**
 * Interface to represent the route
 * of a match and allows to determine
 * if the match is being tested or
 * not.
 */
export interface MatchRouteParams {
    id: string;
    testing: boolean;
}
